<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Server Info</title>
</head>
<body>
    <h1>Информация о сервере</h1>

    <p><strong>Hostname:</strong> {{ ansible_hostname }}</p>
    <p><strong>IP:</strong> {{ ansible_default_ipv4.address }}</p>
    <p><strong>CPU:</strong> {{ ansible_processor_cores }} cores</p>
    <p><strong>RAM:</strong> {{ ansible_memtotal_mb }} MB</p>

    {% set first_disk = ansible_devices | dict2items | first %}
    <p><strong>First disk:</strong> {{ first_disk.key }} ({{ first_disk.value.size }})</p>

</body>
</html>

